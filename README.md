Frontmike | Михаил

# React Project

## Установка зависимостей

Перед первым запуском проекта установите все необходимые зависимости:

```
npm install
```

## Запуск проекта в dev режиме

```
npm run dev
```
## Сборка проекта в build версию

```
npm run build
```

Конвертер что-то криво форматировал вариативный шрифт в woff2, так что пришлось оставить ttf
Алиасы и реэкспорт а файлах компонентов делать не стал. Мне кажется это тут было бы избыточно.
Хуки не писал, т.к. на данном этапе самих задач в макете нет. 
Первый раз пишу на реакте, если что-не так - критикуйте смело.